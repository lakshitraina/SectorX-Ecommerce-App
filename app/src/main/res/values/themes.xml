<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SectorX" parent="Theme.Material3.Light.NoActionBar">
        <!-- Set the status bar color to black -->
        <item name="android:statusBarColor">@android:color/black</item>

        <!-- Optional: Set status bar icons to light color for visibility on dark background -->
        <item name="android:windowLightStatusBar">false</item> <!-- Dark icons in status bar -->

        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SectorX" parent="Base.Theme.SectorX" />
</resources>
